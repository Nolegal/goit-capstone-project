Для дослідження цієї роботи було обрано набір даних Rossmann Store Sales, завантажено файл train.csv(історичні данні включаючи продажі) та store.csv(додаткова інформація про магазини).  Визначено тип даних та кількість пропусків, наявність дублікатів. Далі визначено дні коли магазин був відчинений та були успішні продажі open_train = train[(train['Open'] == 1) & (train['Sales'] > 0)].copy() по конкретним місяцям замість повних дат 
open_train['Month'] = open_train['Date'].dt.month , та візуалізовано розподіл за ознакою 'Sales' (продажі). За допомогою методу  .groupby() візуалізовано середнє значення продажів по дням тижня та середнє значення продажів по місяцям. Виведено загальні статистичні дані по ознаці 'Sales'.Також виведене середнє значення продажів в дні акцій,на шкільних канікулах та дні державних свят.
	Підраховано кількість магазинів за їнім типом , асортиментом та виведені статистичні дані по показнику відстань до конкурента store['CompetitionDistance'].
	Виведена ступінь кореляції за числовими ознаками.
Далі виділяются дані які були виділені в дні коли магазин був відкритий та були наявні продажі також за допомогою df = df.merge(store, on='Store', how='left')об’єднуємо дані про магазин. Тепер у вас є один потужний датафрейм, де кожен рядок — це не просто факт продажу, а повна картина: «Якого числа, в якому типі магазину, з яким асортиментом і за наявності яких конкурентів було зроблено цей продаж». Саме такі дані вже готові для глибокого аналізу та машинного навчання. 
Потім розбивалася дата на число рік місяць та тиждень року, це робиться для виділення всплесків продажів. Пропущені значення відстаней до конкурента df['CompetitionDistance'] заповнювалася максимальним помноженим на два що б показати що конкурент є дуже далеко.  Пропущені значення конукрент відкрився з початку року та початку місяця ставимо нуль. Це дозволяє нам надалі виконувати математичні операції з цими колонками, не отримуючи помилок. Створилася метрика скільки працює конкурент відносно поточної дати продажу   
df['CompetitionOpenMonths'] = np.where( df['CompetitionOpenSinceYear'] > 0, (df['Year'] - df['CompetitionOpenSinceYear']) * 12 + (df['Month'] - df['CompetitionOpenSinceMonth']), 0 ) 
Потім clip(lower=0): Ця функція замінює всі від'ємні числа на нуль, для того що б ті магазини що відкрились рахувались як ті які ще не існують.
 Перетворено числові місяці (1, 2, 3) у скорочені назви ('Jan', 'Feb', 'Mar'). Це потрібно, тому що в колонці PromoInterval місяці записані текстом (наприклад, "Jan,Apr,Jul,Oct")  ,.fillna(''): Замінилися порожні значення (NaN) у графіку акцій на порожні рядки. Це запобігає помилкам при подальшій перевірці тексту. 
Promo2 — це не просто акція, вона працює за циклічним графіком. 
Щоб акція вважалася активною (1), мають виконуватися дві умови одночасно:
df['Promo2'] == 1: Магазин взагалі бере участь у цій програмі.
r['MonthName'] in r['PromoInterval']: Поточний місяць входить у графік акції цього магазину.
Наприклад: Якщо зараз May (травень), а у магазину акційний інтервал "Feb,May,Aug,Nov", то умова поверне True.
Результат перетворювався на ціле число (0 або 1) за допомогою .astype(int).
Виділена тимчасова колонка MonthName, оскільки вона була потрібна лише як допоміжний інструмент для розрахунку. Це допомагає тримати датасет "чистим" і економити пам'ять. 
Декодуються типи магазинів асортимент та державні свята.
Тренувалні та тестові дані розбиваютья за окремою контрольною датою split_date = '2015-06-01'. Визначились дані середніх історичних продажів кожного магазину  store_mean_sales , які об’єднуються з тренувальними та тестовими данними . Пропуски заповнились середніми даними продажів. Вивилась сума пропущених значень в тренувальній та тестовій вибірці.
Далі сформована ознаку AvgCustomers (середня кількість покупців). Вона працює за тим самим принципом, що й попередній StoreMeanSales, але є більш деталізованою. Цей код рахує скільки в середньому покупців приходить у Магазин №1 саме по понеділках, скільки по вівторках і т.д.. Об’єдналася з тренувальним та тестовим набором, заповнились пропуски середнім  значенням
 global_cust_mean = train_fold['Customers'].mean() 
Вививилась кількість пропусків в тренувальній та тестовій вибірках.
	Виділились ознаки на яких модель буде навчатися та цільову змінну . Перевірилась кількість наявних ознак на пропуски. Розподілились дані на : 
X_train / y_train: На цих даних модель буде вчитися знаходити зв'язки.
X_val / y_val: Модель ніколи не бачила цих відповідей під час навчання, тому на них перевірилась  реальна точність.Визначився розмір даних .
	Створився пустий масив results = [] куди додано результати функції .
На початку дослідження було завантажено датасети з файлів train.csv  , store.csv.
Були виведені типи даних файлу train.csv.
Як видно  більшість ознак є чисовими.
Виведені кількість пропущених даних в файлах.
Як видно  у файлі train.csv відсутні пропущені значення, у файлі store.csv  є пропущені значення але вони заповнюються під час генерації ознак.
Виведено кількість дублікатів в обох файлах
Як видно дублікати відсутні.	
В процесі дослідження було створено матрицю кореляції числових ознак.
Як показує Рисунок  високу ступінь кореляції мають ознаки ‘Sales’ та ‘Customers’. Інші ознаки мають відносно низький рівень кореляції між собою.
	Було виведено інформацію важливості ознак.
Видно що за важливістью ознак вагомими є ‘StoreMeanSales’, ‘AvgCustomers’, ‘CompetitionDistance’. Ознаки ‘SchoolHoliday’, ‘StateHoliday’ мають найменший вплив на цільову змінну.
Було отримано метрики показників оцінки машинного навчання.Як видно з результатів дослідження найкращі показники точності має модель XGBRegressor з коефіцієнтом детермінації 0.89 та середньою абсолютною відсотковою помилкою 11.34%. Дуже близькі показники має модель LGBMRegressor коефіцієнт детермінації 0.88 та середньою абсолютною відсотковою помилкою 11.89%. Третє місце зайняла модель  Random Forest Regressor де коефіцієнт детермінації 0.84 та середня абсолютна квадратична помилка 12.11% . Найгірші результати показала модель  Linear Regression з коефіцієнтом детермінації 0.79 та середньою абсолютною відсотковою помилкою 15.46%.
Був виведений розподіл справжніх та прогнозованих даних.
Якщо порівняти розподіли справжніх та прогнозованих даних моделі XGBRegressor то значних змін не спостерігається.
Створено графік залишків моделі XGBRegressor. Розкид залишків не є критичним. Максимальні зачення розподілу 10 000 одиниць . Залишки в 4000-5000 є цілком прийнятними.
Створено графік порівняння справжніх та прогнозованих значень моделі XGBRegressor.Прогнозовані дані близькі до справжніх.





